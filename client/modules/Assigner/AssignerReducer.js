// Import Actions
import {  } from './AssignerActions';

// Initial State
const initialState = {};

const AssignerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AssignerReducer;
